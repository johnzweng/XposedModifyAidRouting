<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="at.zweng.xposed.modifyaidrouting">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <meta-data
                android:name="xposedmodule"
                android:value="true"/>
        <meta-data
                android:name="xposedminversion"
                android:value="82"/>
        <meta-data
                android:name="xposeddescription"
                android:value="Allows &apos;catch-all&apos; routing for NFC Host-Card-Emulation. This module alone does nothing! This is an helper module for app developers. An NFC HCE app developer can use it to receive all SELECT APDUs (not just the ones the app registered for)."/>
    </application>

</manifest>